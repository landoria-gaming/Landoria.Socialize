name: Release package
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  package:
    uses: landoria-gaming/LandoriaModActions/.github/workflows/release.yml@v4.5
    with:
      project-file: Landoria.Socialize.csproj
      mod-name: Landoria.Socialize
    secrets:
      references-token: ${{ secrets.MOD_REFERENCES_TOKEN }}
